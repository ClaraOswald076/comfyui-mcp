- **P1 — [src/tools/model-management.ts:477](C:/Users/Artokun/AppData/Local/Temp/claude/C--Users-Artokun-code-comfyui-mcp/b93c29cc-34cd-4185-a2d4-f60c0dfe11cd/scratchpad/wt-mcp-downloads/src/tools/model-management.ts:477)**: The reclaim response unconditionally says “its panel tray row removed,” but reclaim only makes a best-effort `clearDownloadProgress` call. It is explicitly a no-op when `PROGRESS_DIR` is unset, and it deliberately skips IDs shared by a live job. Thus this reports an unobserved destructive outcome as successful. The new test spies on the call rather than observing removal, so it passes in the no-op configuration that demonstrates the false claim.

- **P2 — [src/services/download-jobs.ts:1267](C:/Users/Artokun/AppData/Local/Temp/claude/C--Users-Artokun-code-comfyui-mcp/b93c29cc-34cd-4185-a2d4-f60c0dfe11cd/scratchpad/wt-mcp-downloads/src/services/download-jobs.ts:1267)**: The live-row guard records only `progressId ?? trayId`, while reclaim clears both identifiers of the dead record. A fresh/live record with `progressId: "new-progress"` and `trayId` equal to the reclaimed record’s tray ID is treated as not sharing that row, so reclaim clears the live download’s tray row. The same faulty preference is used for fresh foreign records at line 1272. This violates the stated “unless a live job shares the row id” invariant; tests cover only the progress-ID-sharing direction, not this inverse shape.